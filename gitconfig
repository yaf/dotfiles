[user]
  name = Yannick François
  email = yannick.francois@beta.gouv.fr

[core]
  editor = vi
  ignorecase = false
  excludesFile = ~/.gitignore

[push]
  default = current

[diff]
  noprefix = true

[filter "lfs"]
  required = true
  clean = git-lfs clean -- %f
  smudge = git-lfs smudge -- %f
  process = git-lfs filter-process

[color]
  ui = true
